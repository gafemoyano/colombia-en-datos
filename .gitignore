node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Database files (local dev only; Turso is used in production)
drizzle/db.sqlite

# Generated canonical store (rebuild with `npm run canonical:rebuild`)
data/observations.duckdb
data/observations.duckdb.next-*

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
tmp/
.worktrees

# Amp orb runtime
.amp/portals/*
