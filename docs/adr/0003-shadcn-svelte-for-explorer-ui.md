# Use shadcn-svelte for the Explorer UI

shadcn-svelte is the UI component foundation for the new Explorer view and for product work going forward. Existing local shadcn-inspired primitives may remain temporarily to preserve compilation, but new product slices should use shadcn-svelte and old primitives should be migrated opportunistically rather than expanded into a second component system.
